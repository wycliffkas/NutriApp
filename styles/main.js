import { StyleSheet } from "react-native";
import { darkGrey200 } from "../styles/colors"

export const main = StyleSheet.create({
	title: {
		fontSize: 24,
		fontWeight: 700,
		color: darkGrey200,
		fontFamily: "EBGaramond-Regular",
		lineHeight: 31.32,
    marginTop: 30,
    // marginBottom: 10
	}
});
